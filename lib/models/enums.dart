enum Currency {
  euro, 
  dollars
}

enum TricountLabel {
  event,
  holidays,
  other,
  project,
  trip,
  work
}

enum TransactionLabel {
  accomodation, food, entertainment, groceries, insurance, rent, other, salary, restaurants, shopping, transport
}