import 'package:flutter/material.dart';

class CreateTricountScreen extends StatelessWidget {
  const CreateTricountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("New Tricount"),),
    );
  }
}